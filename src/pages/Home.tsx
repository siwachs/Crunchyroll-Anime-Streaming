import React from "react";

const Home: React.FC = () => {
  return (
    <>
      {/* {Array.from({ length: 50 }, (_, index) => (
        <h1 key={index}>Test</h1>
      ))} */}
    </>
  );
};

export default Home;
